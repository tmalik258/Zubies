upstream backend {
    server backend:8002;
}

server {
    listen 80;
    server_name zubies.com www.zubies.com 167.172.238.229;
    # Allow ACME challenges for Certbot
    location /.well-known/acme-challenge/ {
        root /data/nginx;
    }
    # Direct proxy to backend - no redirects
    location / {
        proxy_pass http://backend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# server {
#     listen 443 ssl;
#     server_name zubies.com www.zubies.com 167.172.238.229;

#     # Remove these redirects for now, they could be causing the loop
#     # Redirect www to non-www
#     # if ($host = www.zubies.com) {
#     #     return 301 https://zubies.com$request_uri;
#     # }
#     # # Redirect IP to domain
#     # if ($host = 167.172.238.229) {
#     #     return 301 https://zubies.com$request_uri;
#     # }

#     ssl_certificate /etc/letsencrypt/live/zubies.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/zubies.com/privkey.pem;

#     # Handle 497 errors (HTTP requests to HTTPS port)
#     error_page 497 https://$host$request_uri;

#     location / {
#         proxy_pass http://backend;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }

#     # Block WordPress bot requests
#     location ~* /wp-includes/ {
#         return 403;
#     }
# }